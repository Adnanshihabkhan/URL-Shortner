<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: bisque;
            background-color: black;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>

<body>
    <h1>URL-SHORTNER</h1>

    <div>
        <form action="/url" method="POST">
            <label for="">Enter Your Orignal URL </label>
            <input type="text" name="url" placeholder="https://example.com"
                style="margin-left: 10px;margin-right: 10px;">
            <button type="submit"
                style="background-color: aquamarine; color: brown; font-weight: 900;">Generate</button>
        </form>
        <% if (locals.id) { %>
            <p>URL Generated: http://localhost:3000/<%= id %>
            </p>
            <% } %>
                <!-- <div>
                    <% if (locals.urls) { %>
                        <table>
                            <thead>
                                <th>s.no</th>
                                <th>shortid</th>
                                <th>redirectURL</th>
                                <th>cliks</th>
                            </thead>
                            <tbody>
                                <% urls.forEach((url , index)=> { %>
                                    <tr>
                                        <td>
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <%= url.shortId %>
                                        </td>
                                        <td>
                                            <%= url.redirectURL%>
                                        </td>
                                        <td>
                                            <%= url.visitHistory.length%>
                                        </td>
                                    </tr>
                                    <% }) %>

                            </tbody>
                        </table>

                        <% } %>
                </div> -->
    </div>
</body>

</html>